/**
 * @name RequestConfig
 * @typedef RequestConfig
 * @property {string?} url - url to request
 * @property {string} method - request method (get, post, patch, ...)
 * @property {object?} params - request params
 * @property {object?} data - data to send in request body
 */

/**
 * @name MarvelFilter
 * @typedef MarvelFilter
 * @property {number|string} limit - results limit (used for pagination)
 * @property {number|string} offset - results offset (used for pagination)
 */
